# ASIC Notes

This directory holds placeholder artifacts for the future ASIC-oriented flow.

- `openlane_config_placeholder.json` is only a seed for eventual
  OpenLane/OpenROAD configuration.
- `constraints/dma.sdc` currently contains broad timing assumptions that must be
  refined once the clocking, IO environment, and CDC strategy are final.
